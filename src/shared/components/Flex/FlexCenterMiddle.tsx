import styled, { css } from 'styled-components';
import { flexCenter } from './FlexCenter';

export const flexCenterMiddle = css`
  ${flexCenter};
  align-items: center;
`;

export const FlexCenterMiddle = styled.div`
  ${flexCenterMiddle};
`;
